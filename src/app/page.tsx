import { Box } from "@chakra-ui/react";

export default function Home() {
  return (
    <Box>
      {/* 
        1. Create scene
        2. Add the camra
        3. Create objects
        4. Add lighting
        5. Set up the renderer
        6. animate the scene*/}
      Home
    </Box>
  );
}
